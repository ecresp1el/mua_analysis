# This line will allow users to import the NeuralAnalysis class directly from your package
#It's used to allow users to directly import the NeuralAnalysis class from your package level, 
# without needing to specify the module (core.py) where the class is defined.
from .core import NeuralAnalysis
